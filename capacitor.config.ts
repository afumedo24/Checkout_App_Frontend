import { CapacitorConfig } from '@capacitor/cli';


const config: CapacitorConfig = {
  appId: 'io.ionic.checkout.app',
  appName: 'Chechout App',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
